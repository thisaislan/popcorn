namespace Popcorn.Metadatas
{

    public static class Tags
    {

        public enum Persons
        {
            Player,
            Enemy
        }

        public enum Surfaces
        {
            Platform
        }

        public enum Objects
        {
            Hit,
            StartPoint,
            EndPoint,
            BackgroundItem,
            Object
        }

        public enum Elementies
        {
            Camera,
            Limit,
            RightLimitView,
            LeftLimitView,
            UpLimitView,
            BottomLimitView,
            Destructor,
        }

        public enum Helpers
        {
            WeakPoint
        }

        public enum UIElementies
        {
            Time
        }

    }
}
